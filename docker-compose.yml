services:
  bot:
    build: .
    container_name: schedule_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./db.json:/app/db.json
      - ./credentials.json:/app/credentials.json:ro  # <-- ДОДАНО (ro означає Read-Only, лише для читання)
    network_mode: "host"